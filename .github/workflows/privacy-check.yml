name: Privacy Check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject private paths, emails, and credential-like values
        run: ./script/privacy_check.sh
